# Copy to .env.e2e (not committed) and set credentials for a real Supabase test user.
# Password must match Supabase auth requirements and be at least 8 characters.

E2E_EMAIL=test-user@example.com
E2E_PASSWORD=TestUser123!
E2E_FULL_NAME=Beplanned Test User
